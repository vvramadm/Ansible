---
- name: nginx insallation
  hosts: web
  become: yes
  tasks: 
    - name: insall nginx
      ansible.bultin.package:
    - name: nginx
      state: present
    - name: start nginx service
      ansible.bultin.service:
       - name: nginx
         state: started
         enabled: yes
